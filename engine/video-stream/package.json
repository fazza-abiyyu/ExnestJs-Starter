{
  "name": "@exnest/video-engine",
  "version": "0.0.1",
  "description": "Universal HLS video streaming machine for the exnest ecosystem — Bun + Elysia, zero-dep core (one optional runtime dep: youtube-dl-exec for YouTube). Upload a file or give it a URL (Google Drive & YouTube included) and get adaptive-bitrate HLS back. Ships Elysia and NestJS adapters.",
  "type": "module",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit -p tsconfig.json"
  },
  "peerDependencies": {
    "@nestjs/common": ">=8 <12",
    "elysia": ">=1.4"
  },
  "dependencies": {
    "youtube-dl-exec": "^3.1.4"
  },
  "trustedDependencies": [
    "youtube-dl-exec"
  ],
  "devDependencies": {
    "@nestjs/common": "^10.4.15",
    "@nestjs/core": "^11.2.1",
    "@types/node": "^22.10.2",
    "bun-types": "^1.3.14",
    "elysia": "^1.4.24",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  }
}